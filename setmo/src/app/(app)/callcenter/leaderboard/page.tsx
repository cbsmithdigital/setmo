// Floor-manager leaderboard = the shared leaderboard page, which branches on
// user.callCenterPodId → pod + whole-center agent boards (getAgentLeaderboards).
export { default } from "@/app/(app)/leaderboard/page";
